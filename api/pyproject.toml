[project]
name = "api"
version = "0.1.0"
description = "API para predição de salários da NBA"
requires-python = ">=3.14"
dependencies = ["fastapi[standard]>=0.139.0"]

[tool.fastapi]
entrypoint = "main:app"
